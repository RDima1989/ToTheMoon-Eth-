# ToTheMoon-Eth-

This repository now contains a basic automated test for registration and login forms.

## Running the tests

Set the `BASE_URL` environment variable to point at your application under test and run:

```
python -m unittest tests/test_auth_forms.py
```

